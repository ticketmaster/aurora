import React from "react";
import styled from "styled-components";

const ModalWithPanelInfo = ({items}) => (
  <React.Fragment>
    ModalWithPanelInfo
  </React.Fragment>
);

export default ModalWithPanelInfo;